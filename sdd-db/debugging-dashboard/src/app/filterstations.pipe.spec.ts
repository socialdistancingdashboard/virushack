import { FilterstationsPipe } from './filterstations.pipe';

describe('FilterstationsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterstationsPipe();
    expect(pipe).toBeTruthy();
  });
});
